# Image and Video Buffer Input and Output
---
name: VSampler
rates: [ frame, shape, pixel ]
sampler: true
inputs:
    - pos
outputs: [ out ]
dimensions:
    - inputs: [ 2 ]
      outputs: 4
shader: "@out = texture(@sampler, @pos);"

