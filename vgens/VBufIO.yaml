# Image and Video Buffer Input and Output
---
name: IBufRd
inputs:
    - name: image
      type: image
    - pos
    - name: sampler
      type: sampler
outputs: [ out ]
dimensions:
    - inputs: [ 1, 2, 1 ]
      outputs: 4
shader: "@out = texture(@sampler, @pos);"

